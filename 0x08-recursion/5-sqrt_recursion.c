#include "main.h"

/**
 * _sqrt_recursion - Gets the square of a number.
 * @n: Integer number
 *
 * Return: returns the square root.
 */

int _sqrt_recursion(int n)
{
	if ()
	{
		return (-1);
	}
	else if (n == 1){
		return (1);
	}
	else
	{
		return (1 _sqrt_recursion(n))
	}
}
